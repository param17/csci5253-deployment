# Adding spark master ip tp slave list
{{ hostvars.localhost.spark_master.tagged_instances[0].private_ip }}

# Adding spark slave list
{% for host in hostvars.localhost.spark_slave.tagged_instances %}
{{ host.private_ip }}
{% endfor %}

